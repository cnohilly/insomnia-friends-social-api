const { User } = require('../models');

const userController = {
    // get all users

    // get one user by id

    // create a new user

    // update a user by id

    // delete a user by id

}

module.exports = userController;